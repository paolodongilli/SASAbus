<menu
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/action_parking_call"
        android:title="@string/parking_call"
        android:icon="@drawable/ic_call_white_24dp"
        app:showAsAction="ifRoom"/>

    <item
        android:id="@+id/action_parking_navigate"
        android:title="@string/parking_navigate"
        android:icon="@drawable/ic_navigation_white_24dp"
        app:showAsAction="ifRoom"/>

</menu>